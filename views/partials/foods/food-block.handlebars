<li>
	{{burger_name}}

	<button style="background-color: yellow;" class="change-burger" data-id="{{id}}" data-newburger="{{devoured}}">
		{{#if devoured}}Click To Eat!{{else}}Devoured!{{/if}}
	</button>
	
	<button class="delete-burger btn btn-danger" data-id="{{this.id}}">
		Delete!
	</button>
</li>
